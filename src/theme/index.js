import { createTheme } from '@mui/material/styles'

// A custom theme for this app
const theme = createTheme({
  palette: {
    background: {
      default: '#fcf7ee',
    },
  },
})

export { theme }
